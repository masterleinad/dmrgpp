name: CI - MacOS 26

on:
  push:
    branches:
  pull_request:
    branches:
      - master

env:
  BUILD_TYPE: Release

jobs:
  build:
    runs-on: macos-26

    steps:
    - name: Checkout
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
        path: source

    - name: Install Dependencies
      run: brew install boost hdf5-mpi gsl open-mpi

    - name: Checkout Kokkos
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
        repository: kokkos/kokkos
        ref: 5.1.1
        path: kokkos

    - name: Checkout KokkosKernels
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
          repository: kokkos/kokkos-kernels
          ref: 5.1.1
          path: kokkos-kernels

    - name: Install Kokkos and KokkosKernels
      run: |
        cmake -S kokkos \
              -B kokkos/build \
              -S kokkos \
              -G Ninja \
              -DCMAKE_INSTALL_PREFIX=${{github.workspace}}/kokkos-install \
              -DKokkos_ARCH_NATIVE=ON
        cmake --build kokkos/build --target install
        cmake -B kokkos-kernels/build \
              -S kokkos-kernels \
              -G Ninja \
              -DCMAKE_INSTALL_PREFIX=${{github.workspace}}/kokkos-kernels-install \
              -DKokkos_ROOT=${{github.workspace}}/kokkos-install
        cmake --build kokkos-kernels/build --target install

    - name: Configure
      run: |
        cmake -S ${{github.workspace}}/source \
              -B ${{github.workspace}}/build \
              -GNinja \
              -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}} \
              -DCMAKE_CXX_FLAGS="-Wall -Wextra -Wno-unused-parameter" \
              -DCMAKE_COMPILE_WARNING_AS_ERROR=ON \
              -DKokkosKernels_ROOT=kokkos-kernels-install \
              -DUSE_KOKKOS=ON

    - name: Build
      run: cmake --build ${{github.workspace}}/build --config ${{env.BUILD_TYPE}}

    - name: Test
      run: ctest --test-dir ${{github.workspace}}/build -C ${{env.BUILD_TYPE}} -E ^output3$ -LE Nightly --output-on-failure

    - name: Install
      run: cmake --install ${{github.workspace}}/build --prefix ${{github.workspace}}/install --config ${{env.BUILD_TYPE}}

